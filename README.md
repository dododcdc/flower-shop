# 🌺 【花言花语】鲜花售卖系统

为【花言花语】花店开发的在线销售平台，扩展10km范围内的线上销售渠道，实现从纯线下门店到线上线下结合的经营模式升级。

## 技术架构

```
Frontend (React 18 + TypeScript + Vite)
    ↓ HTTP/REST API
Backend (Spring Boot 3.2 + MyBatis-Plus)
    ↓ JDBC
Database (MySQL 8.0)
```

## 技术栈

### 前端 (flower_ui/)
- **React 18.2.0** - UI框架
- **TypeScript 5.9.3** - 类型安全
- **Vite 5.0.0** - 构建工具
- **Material UI 7.3.5** - UI组件库
- **Zustand 4.5.7** - 状态管理
- **React Router 6.15.0** - 路由管理
- **Axios 1.5.0** - HTTP客户端
- **notistack 3.0.2** - 消息提示

### 后端 (flower_server/)
- **Spring Boot 3.2.0** - Web框架
- **Java 21** - 开发语言
- **MyBatis-Plus 3.5.5** - 数据访问层（Spring Boot 3专用版本）
- **MySQL 8.2.0** - 数据库
- **Spring Security + JWT** - 安全认证
- **Maven 3.9+** - 构建工具

## 项目结构

```
flower_shop/
├── flower_ui/                # 前端项目
│   ├── src/
│   │   ├── api/             # API接口封装
│   │   ├── components/      # 公共组件
│   │   ├── pages/           # 页面组件
│   │   │   ├── admin/       # 管理端页面
│   │   │   └── shop/        # 客户端页面
│   │   └── store/           # Zustand状态管理
│   └── package.json
├── flower_server/            # 后端项目
│   ├── src/main/java/com/flower/shop/
│   │   ├── controller/      # 控制器层
│   │   ├── service/         # 服务层
│   │   ├── mapper/          # MyBatis映射器
│   │   ├── entity/          # 实体类
│   │   ├── dto/             # 数据传输对象
│   │   ├── config/          # 配置类
│   │   └── util/            # 工具类
│   └── pom.xml
├── CLAUDE.md                 # Claude Code开发指南
└── README.md                 # 项目说明
```

## 快速开始

### 环境要求

**前端：**
- Node.js 18.0+
- npm 9.0+

**后端：**
- Java 21
- Maven 3.9+
- MySQL 8.0+

### 数据库准备

1. 创建数据库：
```sql
CREATE DATABASE flower_shop CHARACTER SET utf8mb4 COLLATE utf8mb4_unicode_ci;
```

2. 执行初始化脚本：
```bash
mysql -u root -p flower_shop < script.sql
```

### 后端启动

```bash
cd flower_server
mvn clean package
java -jar target/flower-shop-1.0.0.jar
```

后端服务将运行在 `http://localhost:8080`

### 前端启动

```bash
cd flower_ui
npm install
npm run dev
```

前端服务将运行在 `http://localhost:5173`

## 已实现功能

### 管理端（老板端）
- ✅ 管理员登录和JWT认证
- ✅ 商品管理（表格视图，支持CRUD操作）
- ✅ 商品上架/下架
- ✅ 库存管理
- ✅ 仪表板（统计数据展示）

### 客户端（顾客端）
- ✅ 商品展示（按花材分类）
- ✅ 商品详情查看
- ✅ 购物车功能
- ✅ 游客下单（无需注册）
- ✅ 用户注册/登录
- ✅ 心意卡片编辑器（三种风格）
- ✅ 订单查询（手机号查询）
- ✅ 我的订单（登录用户查看历史订单）

### 核心特性
- **游客友好**：无需注册即可下单购买
- **地址验证**：10km配送范围限制
- **实时库存**：下单时自动检查库存
- **订单跟踪**：订单状态实时更新
- **模拟支付**：当前使用模拟支付，待接入真实支付

## 数据库设计

### 核心表结构
- **categories** - 商品分类（花材/包装）
- **products** - 商品信息
- **inventory** - 库存管理
- **orders** - 订单主表
- **order_items** - 订单详情
- **users** - 用户信息
- **delivery_addresses** - 配送地址

### 枚举值规范
- 订单状态：`PENDING`, `PAID`, `PREPARING`, `DELIVERING`, `COMPLETED`, `CANCELLED`
- 支付方式：`ALIPAY`, `WECHAT`, `CASH`, `MOCK`
- 支付状态：`PENDING`, `PAID`, `REFUNDED`
- 用户角色：`ADMIN`, `CUSTOMER`

## 开发命令

### 前端
```bash
cd flower_ui

# 安装依赖
npm install

# 开发模式
npm run dev

# 构建生产版本
npm run build

# 预览构建结果
npm run preview
```

### 后端
```bash
cd flower_server

# 编译项目
mvn compile

# 运行测试
mvn test

# 打包项目
mvn clean package

# 跳过测试打包
mvn clean package -DskipTests

# 运行应用
java -jar target/flower-shop-1.0.0.jar
```

## 关键业务规则

### 商品管理
- 5种花材：玫瑰、百合、夜来香、康乃馨、向日葵
- 2种包装：花束、花篮
- 花篮价格高于花束
- 库存按具体商品管理

### 配送规则
- 仅限10km范围内配送
- 地址自动验证
- 配送时间精确到时段

### 订单流程
- 支付成功后扣减库存
- 新订单立即通知
- 订单状态流转：待付款 → 已付款 → 准备中 → 配送中 → 已完成

## 开发注意事项

### MyBatis-Plus兼容性
⚠️ **重要**：Spring Boot 3.x 必须使用 `mybatis-plus-spring-boot3-starter`，不能使用 `mybatis-plus-boot-starter`

### 认证机制
- 使用JWT Token进行身份认证
- 前端使用拦截器自动添加Token
- 管理端路由需要认证

### 提交规范
- `feat`: 新功能
- `fix`: 修复bug
- `refactor`: 代码重构
- `style`: 代码格式调整
- `docs`: 文档更新

## 未来计划

- [ ] 真实支付接入（支付宝/微信）
- [ ] 购物车数据云端同步
- [ ] 订单批量处理功能
- [ ] 数据统计报表
- [ ] 客户管理系统
- [ ] 移动端适配优化

## 更新日志

### 最近更新
- 重构商品管理为表格视图
- 统一管理端路由前缀
- 升级配送时间系统为双字段模式
- 实现游客订单查询功能
- 完善用户注册登录系统

## 许可证

本项目采用 [MIT License](LICENSE) 开源协议。

---

**项目名称**：花言花语
**开发模式**：单人开发
**最后更新**：2026-01-04
